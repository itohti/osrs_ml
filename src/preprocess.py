import pandas as pd
# We want this function to
# convert all percentages to actual numbers.
# 
def preprocess(users_df, tasks_df):
    # users_df.to_csv('./saved_data/users')
    # tasks_df.to_csv('./saved_data/tasks')

    print(users_df)
    print(tasks_df)